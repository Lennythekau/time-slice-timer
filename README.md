# ⏰🍕 Time Slice Timer

A timer meant for workflows that frequently switch between tasks.

# Setup

Install `Gtk4` (**not** `Gtk3`).

Install the Python libraries, setting up a venv beforehand if you wish:

```bash
pip install -r requirements.txt
```

Run:

```bash
python main.py
```
